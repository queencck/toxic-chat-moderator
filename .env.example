# Read automatically by `docker compose` for ${VAR} substitution in
# docker-compose.yml. NOT passed into containers - services use env_file.
# Copy to .env and fill in.

DB_NAME=toxic_moderator
DB_USER=postgres
DB_PASSWORD=replace-me
DB_HOST=localhost
DB_PORT=5432
REDIS_PORT=6379
NEXT_PUBLIC_API_URL=http://api.localhost:8000
